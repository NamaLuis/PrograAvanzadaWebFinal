@model IEnumerable<UI.Login.Models.Puesto>

@{
    ViewData["Title"] = "Index";
}

<h1>Puestos</h1>

<p>
    <a asp-action="Create">Crear Nuevo</a>
</p>
<table class="table table-dark table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Nombre")
            </th>
            <th>
                @Html.DisplayName("Manager")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Manager)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdPuesto">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.IdPuesto">Detalles</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdPuesto">Borrar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

